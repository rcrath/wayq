# Wire the SVG art into wayq's OUT EQ (match RhythmEcho)

Outcome: wayq's OUT EQ should render the same SVG-based art RhythmEcho does, instead of hand-drawn shapes:
- Output-position route switch uses **oa.svg / ob.svg** (output position).
- "In the feedback loop" route position uses **fa.svg / fb.svg**.
- The Baxandall tilt handles are derived from **yinyang.svg** (the tricky one).

- [x] Read `E:/Dropbox/audio/git/rhythmecho/src/OutFilterRow.cpp` (and its .h) to learn exactly how it uses the SVGs: the oa/ob/fa/fb route-switch art (around lines 207-210) and the yinyang load (around lines 353-354) plus wherever the two Baxandall tilt handles are painted from it. — Route: DrawableButton, setImages(O, .., F-on). Yinyang: drawable split via findChildWithID("yin"=gold=bass / "yang"=cream=treble), scale kYinyangDispR/95, transform maps SVG centre (100,100) to anchor; yang rotation is baked into the SVG. RhythmEcho uses yinyang for active modeX freq + separate edge sliders for gain; wayq has a different combined freq+gain dot model, so per the constraints I keep wayq's dot geometry and swap only the art.
- [x] In wayq's `src/OutFilterRow.cpp`/`.h`, load the Drawables from BinaryData (`oa_svg`, `ob_svg`, `fa_svg`, `fb_svg`, `yinyang_svg`) the same way RhythmEcho does (`juce::Drawable::createFromImageData`). — route SVGs loaded in ctor; `yinyangDrawable_` member loaded at end of ctor.
- [x] Render the route/position switch with oa.svg/ob.svg for the OUTPUT position and fa.svg/fb.svg for the feedback-loop position, matching RhythmEcho's A=a / B=b mapping. Replace the current hand-drawn route button art only. — `routeBtnA_/B_` changed from TextButton to DrawableButton; `setImages(O, .., F-on)` so off=output face, on=feedback face. A uses oa/fa, B uses ob/fb. setButtonText removed; border restored via paintOverChildren.
- [x] Derive the Baxandall bass-tilt and treble-tilt handles from yinyang.svg exactly as RhythmEcho does — match its split/rotation/scaling/placement/colour. This is the delicate part; mirror RhythmEcho's code closely rather than improvising. — Active-channel modeX tilt dots replaced by `paintYinyangHandle`: yin (gold) at bass dot, yang (cream) at treble dot, scale kYinyangDispR/95, transform maps SVG centre (100,100) to anchor, yang rotation baked into SVG. Anchored on wayq's existing combined freq+gain dot (wayq has no top-edge/edge-slider geometry); inactive channel keeps paintTiltDot, same as RhythmEcho.
- [x] Keep changes focused on the SVG wiring; do not refactor unrelated drawing/geometry. Write a short summary of every file + function touched to `.dispatch/tasks/eq-svg-wiring/output.md`. — only OutFilterRow.cpp/.h touched; no geometry/interaction refactor; output.md written.
